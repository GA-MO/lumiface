[project]
name = "lumiface-example-backend"
version = "0.1.0"
description = "Stand-in for the application backend that holds the Lumiface project key. Not part of Lumiface."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "python-multipart>=0.0.9",
]
