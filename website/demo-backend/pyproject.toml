[project]
name = "lumiface-demo-backend"
version = "0.1.0"
description = "Mints liveness sessions for the docs site's live demo; holds the demo project's key. Not part of Lumiface."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
]
