BOOTSTRAP_API_KEY=lf_sk_change-me
BOOTSTRAP_PRESET=balanced
ADMIN_API_KEY=
DEBUG=0
STORE_FRAMES=0
MATCH_THRESHOLD=0.45
SPOOF_THRESHOLD=0.50
SPOOF_HARD_FLOOR=0.30
FLASH_COUNT=3
FLASH_ENFORCE=1
# face_move: the oval (fraction of the frame's shorter side) and how much the face must grow into it
OVAL_WIDTH_FRACTION=0.62
MOVE_MIN_GROWTH=1.15
MOVE_MIN_FILL=0.75
MAX_CHALLENGE_MS=10000
# Retention: purge loop period (0 = off), how long spent sessions stay before purge. Nothing biometric
# outlives a session: the reference photo's embedding is dropped when the device opens the stream.
RETENTION_INTERVAL_SECONDS=300
SESSION_PURGE_GRACE_SECONDS=3600
# Upload caps: whole body, one frame, decoded pixels
MAX_UPLOAD_BYTES=33554432
MAX_FRAME_BYTES=4194304
MAX_IMAGE_PIXELS=20000000
