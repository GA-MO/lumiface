services:
  server:
    build: .
    ports: ["8000:8000"]
    env_file: [.env]
    volumes:
      - ./data:/srv/data
      - insightface-models:/root/.insightface/models
    restart: unless-stopped
volumes:
  insightface-models:
